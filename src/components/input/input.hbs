<input
  type="{{type}}"
  id="{{id}}"
  name="{{name}}"
  class="user-form__input"
  placeholder="{{placeholder}}"
  value="{{value}}"
  {{#if isRequired}}
    required
    aria-required="true"
  {{/if}}
  aria-describedby="{{name}}-error"
/>
